apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "flui-backend.fullname" . }}
  labels:
    {{- include "flui-backend.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
