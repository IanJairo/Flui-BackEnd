/**
 * @swagger
 * components:
 *   schemas:
 *     ErrorResponse:
 *       type: object
 *       properties:
 *         message:
 *           type: string
 *           example: Erro ao processar a requisição
 *         error:
 *           type: string
 *           example: Detalhes técnicos do erro
 */

module.exports = {};
